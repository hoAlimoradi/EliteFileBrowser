/*
package com.alimoradi.elitefilebrowser.splash;

import android.os.Build;
import android.os.Environment;
import android.util.Log;

import com.alimoradi.elitefilebrowser.android_q.AndroidQActivity;

import java.io.File;

public class Test {
    public void trs(){
        final File folder;
        if (1 > 14) {
            folder = new File(Environment.getExternalStorageDirectory().getParent() + "//" + activity.getResources().getString(R.string.app_name));
        } else {
            folder = new File(Environment.getExternalStoragePublicDirectory(DIRECTORY_DOCUMENT).getPath() + "//")
        }

        boolean success = false;
        Log.e("jafar", " folder .exists" + folder.exists());
        if (!folder.exists()) {
            success = folder.mkdir();

        }
    }























}
*/
